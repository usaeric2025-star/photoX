export const IMAGE_COMPRESS = {
  MAX_WIDTH: 1200,
  QUALITY: 0.8,
  THUMB_WIDTH: 300,
  THUMB_QUALITY: 0.5,
};

export const AI_CONFIG = {
  MAX_TOKENS: 1024,
  CONCURRENCY: 5,
};

export const PAGINATION = {
  PAGE_SIZE: 50,
};
