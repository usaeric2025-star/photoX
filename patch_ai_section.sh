sed -i "s/import { AgnesConfigBlock } from '.\/AgnesConfigBlock.js';/import { AgnesConfigBlock } from '.\/AgnesConfigBlock.js';\nimport { GeminiConfigBlock } from '.\/GeminiConfigBlock.js';/" src/features/settings/AISecuritySection.tsx
sed -i "s/(<AgnesConfigBlock.*t={t}\s*\/>)/\\1\n          <GeminiConfigBlock\n            keysStatus={keysStatus}\n            isEditingGemini={isEditingGemini}\n            setIsEditingGemini={setIsEditingGemini}\n            localGeminiKey={localGeminiKey}\n            setLocalGeminiKey={setLocalGeminiKey}\n            geminiModel={geminiModel}\n            setGeminiModel={setGeminiModel}\n            inputClass={inputClass}\n            saveKey={saveKey}\n            handleSaveModel={handleSaveModel}\n            handleTest={handleTest}\n            isSaving={isSavingKey === 'gemini' ? 'gemini' : null}\n            isTesting={isTestingProvider === 'gemini' ? 'gemini' : null}\n            appLang={appLang}\n            t={t}\n          \/>/g" src/features/settings/AISecuritySection.tsx
