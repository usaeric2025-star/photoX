name: Database Health Check
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Check database health
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: bash scripts/check-db-health.sh
